---
# inventory/group_vars/loki_vars.yml
# Biến bổ sung cho Loki & Promtail
# NOTE: loki_version đã được định nghĩa trong all.yml (3.3.2) — KHÔNG override ở đây
# NOTE: loki_push_url đã được định nghĩa trong all.yml — KHÔNG override ở đây
# File này chỉ chứa các biến KHÔNG có trong all.yml

# Promtail version theo loki_version từ all.yml (không cần define riêng)
# promtail_port đã có trong all.yml

# Nếu cần override per-group, chỉ thêm vào đây những biến CHƯA có trong all.yml
# Ví dụ: loki_extra_config, loki_custom_labels, v.v.
