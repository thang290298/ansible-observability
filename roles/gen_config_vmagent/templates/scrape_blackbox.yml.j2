# scrape_blackbox.yml.j2
# Generated by gen_config_vmagent — DO NOT EDIT MANUALLY
# Group: {{ target_group }} | Site: {{ hostvars[groups[target_group][0]].meta.site | default('unknown') }}
# Hosts: {{ groups[target_group] | length }}
# Probed via blackbox exporter at monitoring_lb
{% for host in groups[target_group] | default([]) %}
- targets: ['{{ hostvars[host].meta.ip }}']
  labels:
    hostname: '{{ hostvars[host].meta.host }}'
    site: '{{ hostvars[host].meta.site | default(site | default("unknown")) }}'
    service: 'icmp_probe'
    role: '{{ hostvars[host].meta.role | default("unknown") }}'
    group: '{{ target_group }}'
{% endfor %}
