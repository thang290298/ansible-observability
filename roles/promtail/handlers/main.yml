---
- name: Reload systemd
  ansible.builtin.systemd:
    daemon_reload: true
  become: true

- name: Restart promtail
  ansible.builtin.systemd:
    name: promtail
    state: restarted
  become: true
